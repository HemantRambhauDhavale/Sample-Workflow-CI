name: Print Message

on:
  push:
    branches:
      -'main'

jobs:
  print-message:
    runs-on: Ubuntu-latest

    steps:
      -name: Print Message
      run: echo "new push detect on main branch"
